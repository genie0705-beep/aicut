# 2026-06-26 일일 로그

## 🌅 오전 (08:00~09:00) — 네이버 API 마무리
- 네이버 검색광고 API HMAC-SHA256 서명 방식 확인 완료
  - Secret Key는 **UTF-8 문자열 그대로 HMAC에 사용** (base64 디코딩 ❌)
  - Python 공식 예제(base64 디코딩 없음)와 동일한 방식으로 정상 작동 확인
- 캠페인 목록 조회 성공: `GET /ncc/campaigns`
  - `에이컷_영상편집_검색` (ID: `cmp-a001-01-000000010565267`) — 1개, ELIGIBLE, 일예산 30,000원
- 통계 조회 성공: `GET /stats?id={id}&fields=["impCnt","clkCnt","ctr","cpc","salesAmt"]&datePreset=last7days&timeIncrement=allDays`
  - 7일 데이터 (6/19~6/25): 노출 1,550, 클릭 15, CTR 0.97%, CPC 1,300원, 총비용 19,493원
- 비즈머니 API 404 — 내부망 전용, mock 데이터 유지
- `channels/naver.js` 수정 완료, source=live 정상 저장
- 정이사님께 결과 보고 후 **네이버 채널 완료 승인**

## 🔄 오전 (09:00~10:00) — 방향 전환: IG → 콘텐츠 파이프라인
- 인스타그램 Graph API 연동 계획(v2) **전체 폐기**
- 새 방향: **콘텐츠 파이프라인 구조**
  - 블로그 → 콘텐츠 자산 → 인스타/쓰레드 재가공 배포
  - 통계 ❌, 등록 여부 + 출처 블로그 글만 추적
  - 쓰레드 탭 신규 필요
- 블로그 주제 추천 → **4번 선택: "여름 시즌 오픈, 업종별 영상 마케팅 체크리스트"**

## ✍️ 블로그 작성 (09:00~11:00)
- 블로그 본문 HTML 작성 (4개 업종 체크리스트)
- 이미지 5장 생성: image_gen.js → dark_purple/light_cyan/dark_green/light_pink 테마
- SmartEditor SE4 자동화 테스트 완료 (제목/본문/저장 모두 정상)
- 기존 포스팅 스타일(224324428697) 참고하여 모바일 톤으로 2차 수정
- 기존 포스팅 스타일(224326578253, 월드컵) 참고하여 이미지 스타일 3차 수정

## 📊 대시보드 점검 및 정리 (12:00~13:00)
- 전체 탭/기능별 상태 점검 → 표로 정리
- Hardcoded 값 → `—` (연동 전) 표시로 교체
- 미구현 버튼 → disabled + '준비 중' 처리
- localStorage 옛 값 DB에서 null로 강제 덮어쓰도록 DataStore 수정
- JSON 백업 생성: `aicut_blog_posts`, `aicut_meetings`, `aicut_tasks_v3`, `aicut_activities`

## 🏗️ 블로그 승인 워크플로우 구축 (14:00~16:00)
### DB/서버/스크립트 전면 구현
- **SQLite 스키마 확장**: content_assets + pending_actions 테이블 (정이사님 피드백: localStorage ❌ → SQLite ✅)
- **db/store.js** — 9개 함수 추가 (createAsset, getAsset, getAllAssets, updateAssetStatus, createPendingAction, resolvePendingAction, getNextPendingAction, getPendingActionByAsset, deleteAsset)
- **server.js** — 6개 API 엔드포인트 추가 (GET /api/blog/assets, /api/blog/pending, POST /api/blog/approve, /api/blog/reject, /api/blog/new, /api/blog/process)
- **channels/blog.js** — ①②③ 자동화 스크립트:
  - `--new`: 주제 5개 제안 → pending_actions 등록
  - 일반 실행: status 순차 처리 (topic_proposed → keyword_proposed → draft_writing → draft_review)
  - 승인 완료 시 다음 단계 자동 진행

### 전체 흐름 테스트 ✅✅✅
1. `--new` → 주제 5개 제안 (topic_proposed + draft_approval) ✅
2. 승인 → topic_approved ✅
3. blog.js 재실행 → 키워드 전략 제안 (keyword_proposed + keyword_approval) ✅
4. 승인 → keyword_approved ✅
5. blog.js 재실행 → 본문 5장 생성 + HTML (draft_writing → draft_review) ✅
6. 현재 draft_review (검토 대기) 상태

### 대시보드 UI (F번)
- 블로그 탭에 pipeline 카드 섹션 추가
- Step Flow 시각화 (5단계: 주제→키워드→본문→검토→발행)
- 승인/반려 버튼 연동
- 새 포스트 버튼 활성화

## 🔑 학습/결정사항
- **이미지 스타일**: 월드컵 포스팅 스타일(aicut_worldcup_main.png) = blog_worldcup_v3.js 패턴 확정
- **SMartEditor SE4 자동화 정상 작동 확인** (CDP → iframe → SmartEditor API + 클립보드)
- **네이버 API**: HMAC-SHA256 서명, Secret Key base64 디코딩 금지
- **데이터 관리 피드백**: Node.js 백엔드와 공유하는 데이터는 무조건 SQLite, localStorage는 브라우저 전용

## 📁 파일 변경사항
- `channels/naver.js` — 수정 (source=live, Secret Key utf-8)
- `db/schema.sql` — content_assets + pending_actions 추가
- `db/store.js` — 9개 함수 추가
- `server.js` — 블로그 API 6개 추가
- `channels/blog.js` — 신규 (워크플로우 자동화)
- `aicut_marketing_dashboard.html` — DataStore null 처리, 버튼 disabled, pipeline UI
- `backup/aicut_blog_posts.json` + `aicut_meetings.json` + `aicut_tasks_v3.json` + `aicut_activities.json` — 백업
