const WebSocket = require('ws');
const http = require('http');

const CONTENT = `월드컵과 영상 콘텐츠 수요 폭발

2026 월드컵이 한창입니다. 경기장의 열기가 SNS로도 이어지면서 숏폼 콘텐츠 수요가 폭발적으로 증가하고 있습니다. 응원 릴스, 경기 하이라이트, 선수 인터뷰 영상 등 월드컵 관련 숏폼이 가장 빠르게 확산되는 콘텐츠입니다. 브랜드와 마케터라면 지금 이 기회를 놓쳐서는 안 됩니다. 월드컵 기간 동안 SNS 사용량은 평소보다 30% 이상 증가하며, 특히 실시간 경기 중에는 숏폼 콘텐츠 소비가 급증합니다. 사용자들은 경기장 밖에서도 모바일로 생생한 현장감을 느끼고 싶어합니다.

왜 숏폼 마케팅이 중요한가

숏폼 콘텐츠는 짧은 시간 안에 강한 인상을 남길 수 있습니다. 사용자의 집중 시간이 짧아진 모바일 환경에서 15초에서 60초 영상은 브랜드 메시지를 효과적으로 전달하는 최적의 포맷입니다. 특히 월드컵 기간에는 감정적 몰입도가 높아 숏폼 콘텐츠의 바이럴 효과가 극대화됩니다. 경기 직후 골 장면이나 선수 인터뷰는 실시간으로 공유되며 엄청난 조회수를 기록합니다. 긴급한 순간의 감정을 담은 숏폼은 사용자들의 공유 욕구를 자극합니다. 브랜드가 자연스럽게 녹아든 콘텐츠는 광고보다 훨씬 높은 호감도를 얻을 수 있습니다.

타깃 설정과 콘텐츠 전략

월드컵 숏폼 마케팅의 첫 단계는 명확한 타깃 설정입니다. 축구 팬만을 대상으로 할 것인지, 일반 대중까지 확장할 것인지에 따라 콘텐츠 톤이 달라집니다. 핵심 액션을 강조한 15초 영상과 스토리텔링을 담은 30초에서 60초 영상을 조합하면 효과적입니다. 경기 일정에 맞춰 사전 콘텐츠, 실시간 콘텐츠, 사후 리뷰 콘텐츠로 나누어 꾸준히 업로드하는 것이 좋습니다. 또한 각 국가별 경기 일정에 맞춰 타겟팅된 콘텐츠를 제작하면 더 높은 참여율을 기대할 수 있습니다. 경기 전날 예측 콘텐츠, 경기 당일 하이라이트, 다음 날 분석 콘텐츠로 이어지는 시리즈 기획이 효과적입니다.

에이컷의 숏폼 제작 노하우

에이컷은 AI 기반 영상 편집 도구로 누구나 쉽게 숏폼 콘텐츠를 제작할 수 있습니다. 자동 자막 생성, 템플릿 제공, 원클릭 편집 기능으로 월드컵 관련 영상을 빠르게 제작할 수 있습니다. 드라마틱한 골 장면, 선수 표정 클로즈업, 응원석 열기 등을 강조한 편집이 좋은 반응을 얻고 있습니다. 특히 에이컷의 AI 기능은 반복 작업을 자동화하여 제작 시간을 대폭 단축시켜 줍니다. 영상 편집에 시간을 덜 쓰고 마케팅 전략에 더 집중할 수 있다는 것이 가장 큰 장점입니다. 편집 경험이 없는 초보자도 에이컷의 가이드 기능을 따라하면 전문가 수준의 숏폼을 만들 수 있습니다.

업로드 최적화와 해시태그 전략

각 플랫폼의 특성에 맞춰 콘텐츠를 최적화해야 합니다. 인스타그램 릴스는 세로형 9대 16 비율, 유튜브 쇼츠는 60초 이하, 틱톡은 15초에서 30초가 적합합니다. 해시태그는 월드컵 관련 인기 태그를 포함하되 브랜드 고유 태그도 함께 사용하여 검색 노출을 높이는 것이 중요합니다. 업로드 시간대도 중요합니다. 경기 직후나 저녁 시간대가 참여율이 가장 높습니다. 플랫폼별 알고리즘을 이해하고 콘텐츠를 최적화하면 유기적 도달률을 크게 높일 수 있습니다. 각 플랫폼의 트렌드 사운드와 챌린지를 활용하는 것도 효과적인 방법입니다.

성과 측정과 피드백

숏폼 마케팅의 성과는 조회수, 좋아요, 공유 수, 댓글 참여율 등으로 측정합니다. 특히 공유율은 콘텐츠의 바이럴 가능성을 보여주는 핵심 지표입니다. A/B 테스트를 통해 어떤 포맷과 메시지가 가장 높은 참여를 이끌어내는지 지속적으로 개선해야 합니다. 인사이트를 바탕으로 다음 콘텐츠를 기획하면 마케팅 효율이 지속적으로 상승합니다. 월드컵 기간 동안 데이터 기반 의사 결정이 성공의 열쇠입니다. 매주 성과를 리뷰하고 전략을 조정하는 과정이 필요합니다.

마무리

월드컵 열기가 한창인 지금이 바로 숏폼 마케팅에 투자할 최적의 시기입니다. 에이컷과 함께라면 누구나 쉽게 영향력 있는 숏폼 콘텐츠를 만들어 월드컵 마케팅에 성공할 수 있습니다. 지금 바로 에이컷으로 첫 숏폼을 제작해 보세요. 월드컵의 열기를 여러분의 브랜드 성장으로 연결하십시오. 경기가 끝난 후에도 팬들의 관심은 지속되므로 꾸준한 콘텐츠 발행이 중요합니다.`;

const TITLE = '2026 월드컵, 숏폼 마케팅으로 응원 열기 올리는 법';

http.get('http://localhost:9224/json', res => {
  let d='';
  res.on('data', c => d+=c);
  res.on('end', () => {
    const tabs = JSON.parse(d);
    const target = tabs.find(t => t.url && t.url.includes('Redirect=Write'));
    if (!target) { console.error('Target not found'); process.exit(1); }
    console.log('Target:', target.title);
    console.log('Title:', TITLE);
    console.log('Content length:', CONTENT.length, 'chars\n');

    const ws = new WebSocket(target.webSocketDebuggerUrl);
    let msgId = 1;
    let checklist = {
      titleSet: false,
      contentWritten: false,
      textLen: 0,
      noHtmlTags: false,
      imageStatus: '미등록 - 수동 등록 필요',
      hashtags: '수동 등록 필요',
      cta: '본문 내 포함됨',
      saved: false
    };

    function send(method, params) {
      params = params || {};
      return new Promise(resolve => {
        const id = msgId++;
        const handler = msg => {
          const data = JSON.parse(msg.toString());
          if (data.id === id) {
            ws.removeListener('message', handler);
            resolve(data);
          }
        };
        ws.on('message', handler);
        ws.send(JSON.stringify({id, method, params}));
      });
    }

    // Get SmartEditor instance + evaluate expression in PostWriteForm iframe
    // All script share code that gets the 'ed' variable from SmartEditor
    const PREAMBLE = `
      var f = document.getElementById('mainFrame');
      var fw = f.contentWindow;
      var ed = fw.SmartEditor.getEditor && fw.SmartEditor.getEditor();
      if (!ed) {
        var eds = fw.SmartEditor._editors;
        ed = eds && (eds.length ? eds[0] : Object.values(eds)[0]);
      }
    `;

    // Execute code in the iframe context (code can reference 'ed')
    async function edExec(code) {
      const r = await send('Runtime.evaluate', {
        expression: `(function(){${PREAMBLE}${code}})()`,
        returnByValue: true
      });
      // r.result.result is the RemoteObject
      return (r.result && r.result.result) || null;
    }

    // Evaluate expression and return the value
    async function edEval(expr) {
      const r = await send('Runtime.evaluate', {
        expression: `(function(){${PREAMBLE}return (${expr});})()`,
        returnByValue: true
      });
      return (r.result && r.result.result) || null;
    }

    function escapeJS(str) {
      return str.replace(/\\/g, '\\\\').replace(/'/g, "\\'").replace(/\n/g, '\\n').replace(/\r/g, '\\r');
    }

    ws.on('open', async () => {
      console.log('Connected to browser CDP\n');

      try {
        // ---- Step 1: Reset document data ----
        console.log('[1/8] Resetting document data...');
        const res1 = await edExec('ed._documentService.resetDocumentData(); return "ok";');
        console.log('  result:', JSON.stringify(res1));

        // ---- Step 2: Set title ----
        console.log('\n[2/8] Setting title...');
        const escapedTitle = escapeJS(TITLE);
        const res2 = await edExec(`ed.setDocumentTitle('${escapedTitle}'); return "ok";`);
        console.log('  title set result:', JSON.stringify(res2));

        // Verify title
        const titleCheck = await edEval('ed.getDocumentTitle()');
        console.log('  Title verification:', JSON.stringify(titleCheck));
        checklist.titleSet = titleCheck && titleCheck.value === TITLE;
        if (checklist.titleSet) console.log('  Title verified OK');

        // ---- Step 3: Focus to first component ----
        console.log('\n[3/8] Focusing to first component...');
        const res3 = await edExec('ed._canvasScrollingService.focusToFirstComp(); return "ok";');
        console.log('  focus result:', JSON.stringify(res3));

        // ---- Step 4: Write content ----
        console.log('\n[4/8] Writing content paragraph by paragraph...');
        const paragraphs = CONTENT.split('\n\n').filter(p => p.trim());
        let totalWritten = 0;

        for (let i = 0; i < paragraphs.length; i++) {
          const p = paragraphs[i].trim();
          if (!p) continue;
          const escaped = escapeJS(p);
          const writeRes = await edExec(`ed._editingService.writeTextWithSoftLineBreak('${escaped}'); return "ok";`);
          if (writeRes && writeRes.value === 'ok') {
            totalWritten += p.length;
            console.log(`  [${i+1}/${paragraphs.length}] ${p.substring(0, 45)}... (${p.length} chars)`);
          } else {
            console.log(`  [${i+1}/${paragraphs.length}] FAILED:`, JSON.stringify(writeRes));
          }
          await new Promise(r => setTimeout(r, 200));
        }
        console.log(`  Total written: ${totalWritten} chars`);

        // ---- Step 5: Check text length ----
        console.log('\n[5/8] Checking text length...');
        const textLenRes = await edEval('ed._documentService.getContentText().length');
        if (textLenRes && textLenRes.value !== undefined) {
          checklist.textLen = textLenRes.value;
        }
        console.log('  textLen:', checklist.textLen);
        if (checklist.textLen >= 100) {
          checklist.contentWritten = true;
        } else {
          console.log('  WARNING: text too short, may need retry');
        }

        // ---- Step 6 & 7: Verify no HTML tag visibility ----
        console.log('\n[6/8] Checking for HTML tag visibility...');
        const contentCheck = await edEval(`
          (function() {
            var text = ed._documentService.getContentText() || '';
            return {
              textLen: text.length,
              hasHtmlTags: /<[a-z][\\s\\S]*>/i.test(text),
              hasStyle: text.indexOf('style=') !== -1,
              hasPTag: /<p[>\\s]/i.test(text),
              hasDivTag: /<div[>\\s]/i.test(text),
              hasBrTag: /<br[>\\s]/i.test(text),
              snippet: text.substring(0, 200)
            };
          })()
        `);
        console.log('  Content analysis:', JSON.stringify(contentCheck));
        const cv = contentCheck && contentCheck.value;
        checklist.noHtmlTags = cv && !cv.hasHtmlTags && !cv.hasStyle && !cv.hasPTag && !cv.hasDivTag;
        if (checklist.noHtmlTags) {
          console.log('  No HTML tags detected. Content is clean.');
        } else {
          console.log('  WARNING: HTML tags may be present!');
        }

        // ---- Step 8: Find and click save button ----
        console.log('\n[7/8] Looking for save button...');
        const saveInfo = await send('Runtime.evaluate', {
          expression: `
            (function() {
              var f = document.getElementById('mainFrame');
              var fw = f.contentWindow;
              var buttons = Array.from(fw.document.querySelectorAll('button'));
              return buttons.filter(function(b) {
                var t = (b.textContent || '').trim();
                return t === '저장';
              }).map(function(b) {
                return {text: (b.textContent || '').trim(), className: b.className, id: b.id};
              });
            })()
          `,
          returnByValue: true
        });
        const saveBtnsValue = saveInfo.result && saveInfo.result.result && saveInfo.result.result.value;
        console.log('  Save buttons found:', JSON.stringify(saveBtnsValue));
        
        if (saveBtnsValue && saveBtnsValue.length > 0) {
          const btnInfo = saveBtnsValue[0];
          console.log('  Clicking button:', btnInfo.text);
          
          const clickRes = await send('Runtime.evaluate', {
            expression: `
              (function() {
                var f = document.getElementById('mainFrame');
                var fw = f.contentWindow;
                var buttons = Array.from(fw.document.querySelectorAll('button'));
                for (var b of buttons) {
                  var t = (b.textContent || '').trim();
                  if (t === '저장') {
                    b.click();
                    return 'clicked: ' + t;
                  }
                }
                return 'no save button found';
              })()
            `,
            returnByValue: true
          });
          const clickValue = clickRes.result && clickRes.result.result && clickRes.result.result.value;
          console.log('  Click result:', JSON.stringify(clickValue));
          checklist.saved = clickValue && clickValue.indexOf('clicked') === 0;
        } else {
          console.log('  No save buttons found - manual click required');
          checklist.saved = false;
        }

        // ---- Step 8 (alt): Directly try clicking using the className we found ----
        if (!checklist.saved) {
          console.log('\n[8/8] Trying alternative save method...');
          const altClickRes = await send('Runtime.evaluate', {
            expression: `
              (function() {
                var f = document.getElementById('mainFrame');
                var fw = f.contentWindow;
                var btn = fw.document.querySelector('button.save_btn__bzc5B');
                if (btn) { btn.click(); return 'clicked save_btn__bzc5B'; }
                var btn2 = fw.document.querySelector('button.publish_btn__m9KHH');
                if (btn2) { btn2.click(); return 'clicked publish'; }
                return 'no button with class found';
              })()
            `,
            returnByValue: true
          });
          const altClickValue = altClickRes.result && altClickRes.result.result && altClickRes.result.result.value;
          console.log('  Alternative click result:', JSON.stringify(altClickValue));
          if (altClickValue && altClickValue.indexOf('clicked') === 0) {
            checklist.saved = true;
          }
        }

      } catch (err) {
        console.log('\nERROR:', err.message);
        console.log(err.stack);
      }

      // ---- Final Report ----
      console.log('\n' + '='.repeat(55));
      console.log('  FINAL CHECKLIST REPORT');
      console.log('='.repeat(55));
      console.log('| 항목                          | 상태                            |');
      console.log('|:------------------------------|:-------------------------------|');
      console.log('| 제목 설정                     | ' + (checklist.titleSet ? '완료' : '실패').padEnd(31) + '|');
      console.log('| 본문 입력 (textLen: ' + String(checklist.textLen).padEnd(4) + ') | ' + (checklist.contentWritten ? '완료' : '재시도 필요').padEnd(31) + '|');
      console.log('| HTML 태그 노출 없음           | ' + (checklist.noHtmlTags ? '완료' : '실패').padEnd(31) + '|');
      console.log('| 이미지 등록                   | ' + '미등록 - 수동 등록 필요'.padEnd(31) + '|');
      console.log('| 해시태그 30개                 | ' + '수동 등록 필요'.padEnd(31) + '|');
      console.log('| CTA 3종                      | ' + '본문 내 포함됨'.padEnd(31) + '|');
      console.log('| 저장 완료                     | ' + (checklist.saved ? '완료' : '직접 클릭 필요').padEnd(31) + '|');
      console.log('='.repeat(55));

      ws.close();
    });
  });
}).on('error', e => console.error('Error:', e.message));
